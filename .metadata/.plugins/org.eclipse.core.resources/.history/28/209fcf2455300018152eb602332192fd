package parser;

import java.util.LinkedList;
import objects.FOLTerm;

public class FOLResolutionEngine
{
    public LinkedList<FOLTerm> compareToTermsList = new LinkedList<FOLTerm>();
    
    public FOLMatchEnum checkTermAgainstList(FOLTerm toCheck)
    {
        FOLMatchEnum result = FOLMatchEnum.FullMatch;
        for (int i = 0; i<compareToTermsList.size(); i++)
        {
            //if(toCheck.strictCompareTo(compareToTermsList.get(i)) == FOLMatchEnum.
            {
                
            }
        }
        return result;
    }
}
